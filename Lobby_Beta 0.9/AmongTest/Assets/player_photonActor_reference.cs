using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class player_photonActor_reference : MonoBehaviour
{
    public class photonActor
    {

    }
    IDictionary<int,photonActor> myPlayerChoices = new Dictionary<int,photonActor>();

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

