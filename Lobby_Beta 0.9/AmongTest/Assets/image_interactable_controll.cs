using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class image_interactable_controll : MonoBehaviour
{
    public GameObject iconInteractable = null;
    // Start is called before the first frame update
    void Start()
    {
        
    }
    public static void CallInteractableIcon()
    {
        //iconInteractable.SetActive(true);
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
